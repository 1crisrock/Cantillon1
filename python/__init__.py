"""Cantillon Python metric engines (Apps A, B, C) + shared data layer.

Submodules:
  data_loader          — API-first normalization + seedData.js fallback
  cantillon_metrics    — App A: CMPI, FPI
  marxian_metrics      — App B: s/v, c/v, rate of profit
  reproduction_metrics — App C: Super-Sankey departmental pipelines
"""

__version__ = "0.1.0"
